var x = "mi index";

var show = function () {
    console.log(x);
}

//depende del orden en que se adjunte el archivo js en el script de html, ya que la variable x con el mismo nombre en ambos casos de sobreescribe ya que es una variable GLOBAL